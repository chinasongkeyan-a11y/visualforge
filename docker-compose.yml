services:
  visualforge:
    build: .
    container_name: visualforge
    ports:
      - "7777:7777"
    volumes:
      # Persist rendered videos to host
      - ./renders:/app/renders
    environment:
      - NODE_ENV=production
      - PORT=7777
      - RENDERS_DIR=/app/renders
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
